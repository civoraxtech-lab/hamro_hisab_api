run command after installation docker 
run:
"docker compose up --build -d"
stop:
"docker compose down -v"

checkout url
for api docs
http://localhost:5000/docs

for gui database
http://localhost:8080/

ready 
